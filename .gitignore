.DS_Store
__pycache__/

# pi credentials and churn. auth.json holds live OAuth access + refresh tokens
# for every logged-in provider. These patterns are a guard, not the mechanism:
# pi/ is populated by copying named files, never the whole agent dir.
pi/auth.json
pi/trust.json
pi/sessions/
pi/npm/node_modules/
pi/models-store.json
